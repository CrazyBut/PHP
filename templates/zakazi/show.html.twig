{% extends 'base.html.twig' %}

{% block title %}Zakazi{% endblock %}

{% block body %}
    <h1>Zakazi</h1>

    <table class="table">
        <tbody>

        <tr>
            <td>{{ zakazi.id }}</td>
            <td>{{ zakazi.Tovar }}</td>
            <td>
                <a href="{{ path('zakazi_show', {'id': zakazi.id}) }}">show</a>
                <a href="{{ path('zakazi_edit', {'id': zakazi.id}) }}">edit</a>
            </td>
        </tr>
    </table>

    <a href="{{ path('zakazi_index') }}">back to list</a>

    <a href="{{ path('zakazi_edit', {'id': zakazi.id}) }}">edit</a>

    {{ include('zakazi/_delete_form.html.twig') }}
{% endblock %}
