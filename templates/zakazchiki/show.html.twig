{% extends 'base.html.twig' %}

{% block title %}Zakazchiki{% endblock %}

{% block body %}
    <h1>Zakazchiki</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zakazchiki.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ zakazchiki.Name }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ zakazchiki.Score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('zakazchiki_index') }}">back to list</a>

    <a href="{{ path('zakazchiki_edit', {'id': zakazchiki.id}) }}">edit</a>

    {{ include('zakazchiki/_delete_form.html.twig') }}
{% endblock %}
