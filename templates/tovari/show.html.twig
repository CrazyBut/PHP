{% extends 'base.html.twig' %}

{% block title %}Tovari{% endblock %}

{% block body %}
    <h1>Tovari</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tovari.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tovari.Name }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ tovari.Price }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tovari_index') }}">back to list</a>

    <a href="{{ path('tovari_edit', {'id': tovari.id}) }}">edit</a>

    {{ include('tovari/_delete_form.html.twig') }}
{% endblock %}
