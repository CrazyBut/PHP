{% extends 'base.html.twig' %}

{% block title %}Edit Tovari{% endblock %}

{% block body %}
    <h1>Edit Tovari</h1>

    {{ include('tovari/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('tovari_index') }}">back to list</a>

    {{ include('tovari/_delete_form.html.twig') }}
{% endblock %}
